# Optional: only set this if auto-detection fails (e.g. Expo tunnel) or you use a custom port.
# Must be reachable from the device running the app:
# - Android emulator: http://10.0.2.2:5000/api
# - Physical phone / iOS: http://YOUR_PC_LAN_IP:5000/api (same Wi‑Fi as the phone)
# Leave unset to use the same host as Metro (recommended for Expo Go on a real device).
#
# EXPO_PUBLIC_API_URL=http://192.168.1.10:5000/api
