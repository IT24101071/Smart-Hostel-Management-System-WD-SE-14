# Optional. If unset in dev, the app uses Metro’s host (Expo Go) or 10.0.2.2 (Android emulator).
# Use your PC’s LAN IP for a manual override, e.g. http://192.168.1.42:3000/api
# EXPO_PUBLIC_API_URL=http://localhost:3000/api